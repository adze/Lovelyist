<div class="user_info">
	<a href="<%= user_path(current_user)%>">
		<span class="user_name">
			<%= current_user.name %>
		</span>
		<span class="products">
			<%= pluralize(current_user.products.count, "product") %>
		</span>
	</a>
</div>